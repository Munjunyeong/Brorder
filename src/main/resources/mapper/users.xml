<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="users">

	<insert id="register">
		INSERT INTO users
		(id, password, name, phone, role, created_data)
		VALUES
		(#{id}, #{password}, #{name}, #{phone}, #{role}, CURDATE())
	</insert>
	
	<select id="item" resultType="kr.com.brorder.users.Users">
		SELECT * FROM users
		WHERE id=#{id}
	</select>
	
	<select id="findid" resultType="kr.com.brorder.users.Users">
		SELECT id FROM users
		WHERE phone=#{phone} AND name=#{name}
	</select>
	
	<select id="findpw" resultType="kr.com.brorder.users.Users">
		SELECT password FROM users
		WHERE id=#{id} AND phone=#{phone}
	</select>
	
	<update id="update">
		UPDATE users
		SET name=#{name}, phone=#{phone}
		WHERE id=#{id}
	</update>
	
	<select id="address" resultType="kr.com.brorder.users.Address">
		SELECT * FROM user_address
		WHERE user_id=#{userid}
	</select>

</mapper>