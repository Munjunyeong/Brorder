<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.com.brorder.order.mapper.OrderMenuMapper">

    <!-- =========================
         ORDER MENU INSERT
         ========================= -->
    <insert id="insert">
        INSERT INTO order_menu(
            order_id,
            menu_id,
            option_id,
            price
        )
        VALUES(
                  #{orderId},
                  #{menuId},
                  #{optionId},
                  #{price}
              )
    </insert>

    <select id="listByOrderId"
            resultType="kr.com.brorder.order.model.OrderMenu">

        SELECT
            order_menu_id AS orderMenuId,
            order_id      AS orderId,
            menu_id       AS menuId,
            option_id     AS optionId,
            price         AS price
        FROM order_menu
        WHERE order_id = #{orderId}

    </select>
    <delete id="deleteByOrderId">
        DELETE FROM order_menu
        WHERE order_id = #{orderId}
    </delete>

</mapper>