<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.com.brorder.review.mapper.ReviewMapper">

    <!-- 조인 결과물이 첫 번째 데이터로 강제 복제되던 현상을 구별해 줄 고유 식별 단추(<id>) resultMap 추가 지정 -->
    <resultMap id="ReviewResponseMap" type="kr.com.brorder.review.domain.ReviewResponseDTO">
        <id property="reviewId" column="reviewId"/>
        <result property="storeId" column="storeId"/>
        <result property="menuId" column="menuId"/>
        <result property="rating" column="rating"/>
        <result property="content" column="content"/>
        <result property="picture" column="picture"/>
        <result property="createdData" column="createdData"/>
        <result property="userIdName" column="userIdName"/>
        <result property="menuName" column="menuName"/>
    </resultMap>

    <select id="findById" parameterType="int" resultType="kr.com.brorder.review.domain.Review">
        SELECT
        review_id    AS reviewId,
        store_id     AS storeId,
        user_id      AS userId,
        menu_id      AS menuId,
        rating       AS rating,
        content      AS content,
        picture      AS picture,
        created_data AS createdData
        FROM review
        WHERE review_id = #{reviewId}
    </select>

    <!-- 기존의 resultType 스펙을 걷어내고 resultMap="ReviewResponseMap" 구조로 변환 매핑 통로 교체 -->
    <select id="findByStoreId" parameterType="int" resultMap="ReviewResponseMap">
        SELECT
        r.review_id    AS reviewId,
        r.store_id     AS storeId,
        r.menu_id      AS menuId,
        r.rating       AS rating,
        r.content      AS content,
        r.picture      AS picture,
        r.created_data AS createdData,
        u.id           AS userIdName,
        m.name         AS menuName
        FROM review r
        JOIN users u ON r.user_id = u.user_id
        JOIN menu m  ON r.menu_id = m.menu_id
        WHERE r.store_id = #{storeId}
        ORDER BY r.created_data DESC
    </select>

    <!-- 마이페이지 내역 조회 쿼리 영역도 똑같이 뭉개지지 않도록 resultMap 구조로 완벽 동기화 처리 -->
    <select id="findByUserId" parameterType="int" resultMap="ReviewResponseMap">
        SELECT
        r.review_id    AS reviewId,
        r.store_id     AS storeId,
        r.menu_id      AS menuId,
        r.rating       AS rating,
        r.content      AS content,
        r.picture      AS picture,
        r.created_data AS createdData,
        u.id           AS userIdName,
        m.name         AS menuName
        FROM review r
        JOIN users u ON r.user_id = u.user_id
        JOIN menu m  ON r.menu_id = m.menu_id
        WHERE r.user_id = #{userId}
        ORDER BY r.created_data DESC
    </select>

    <insert id="save" parameterType="kr.com.brorder.review.domain.Review">
        INSERT INTO review (
        store_id,
        user_id,
        menu_id,
        rating,
        content,
        picture,
        created_data
        ) VALUES (
        #{storeId},
        #{userId},
        #{menuId},
        #{rating},
        #{content},
        #{picture},
        NOW()
        )
    </insert>

    <delete id="deleteById" parameterType="int">
        DELETE FROM review
        WHERE review_id = #{reviewId}
    </delete>

    <update id="update" parameterType="kr.com.brorder.review.domain.Review">
        UPDATE review
        SET
        rating  = #{rating},
        content = #{content},
        picture = #{picture}
        WHERE
        review_id = #{reviewId}
    </update>

</mapper>